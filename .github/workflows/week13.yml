name: Lab 13 - Idiomatic Rust

on:
  push:
    paths:
      - 'week13/**'
      - '.github/workflows/week13.yml'
  pull_request:
    paths:
      - 'week13/**'
      - '.github/workflows/week13.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lab-13-${{ github.ref }}
  cancel-in-progress: true

jobs:
  template:
    if: github.repository == 'bgreenwell/is4010-labs'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Validate starter structure
        run: |
          test -f week13/lab13.md
          test -f week13/Cargo.toml
          test -d week13/src

  grade:
    if: github.repository != 'bgreenwell/is4010-labs'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: stable
      - name: Run Lab 13 tests
        working-directory: week13
        run: cargo test --verbose
      - name: Check formatting
        working-directory: week13
        run: cargo fmt --check
      - name: Run Clippy
        working-directory: week13
        run: cargo clippy -- -D warnings

